export default class DatabaseModel {
  models: { model; stream; listeners: {} };
  objects: { model; stream; listeners: {} };
  attachments: { model; stream; listeners: {} };

  constructor(db) {
    // Todo: let this work over a package.
  }
}
